import React from 'react';
import ReactDOM from 'react-dom/client';
import SpiritCoach from './SpiritCoach';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SpiritCoach />
  </React.StrictMode>
);
